[project]
name = "useful-scripts"
version = "0.1.0"
description = "A collection of useful Python scripts and utilities"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "init_project",
    "md_docs_parser",
]
