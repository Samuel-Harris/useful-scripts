[project]
name = "md-docs-parser"
version = "0.1.0"
description = "Parse markdown documentation into a structured directory hierarchy"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
